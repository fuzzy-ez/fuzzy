{
  "name": "@fuzzy/icons",
  "version": "0.0.1",
  "description": "",
  "author": "priority",
  "license": "MIT",
  "main": "index.js",
  "bin": {
    "fuzzy-icons": "./bin/index.js"
  },
  "files": [
    "dist",
    "bin"
  ],
  "scripts": {
    "build": "tsc && fuzzy-icons build && pnpm run --filter \"../utils\" build",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "webfont": "9.0.0"
  }
}
